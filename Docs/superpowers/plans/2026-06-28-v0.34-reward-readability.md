# v0.34 Reward Readability Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add category labels, value previews, a steadier reward category mix, and run growth summary feedback to the existing room reward flow.

**Architecture:** Extend `RunRewardChoice` as the data boundary, then keep `RunManager` responsible for category rules, preview creation, and applied growth counts. UI classes render prepared fields instead of duplicating reward logic.

**Tech Stack:** Unity 6000, C#, NUnit EditMode tests, Unity MCP verification.

---

### Task 1: RED Tests

**Files:**
- Create: `Assets/Editor/Tests/RewardReadabilityTests.cs`

- [ ] Write tests for category labels, preview lines, category-mixed rewards, and growth summary.
- [ ] Run only `RewardReadabilityTests` and confirm failures mention missing readable reward fields or growth summary.

### Task 2: Reward Choice Payload

**Files:**
- Modify: `Assets/_Game/Scripts/Roguelike/RunRewardChoice.cs`

- [ ] Add `RunRewardCategory`.
- [ ] Add `Category`, `CategoryLabel`, `PreviewLine`, and `GrowthTag` to `RunRewardChoice`.
- [ ] Keep the existing three-argument constructor by forwarding to the expanded constructor.

### Task 3: Reward Creation and Mix

**Files:**
- Modify: `Assets/_Game/Scripts/Roguelike/RunManager.cs`

- [ ] Add reward category rules.
- [ ] Add runtime preview builders for player damage, move speed, dash cooldown, recovery, dodge boost, Guard opening, AI cooldown, rescue health, and build level.
- [ ] Adjust `PrepareRewardChoices` so Battle/Elite/Shop choices include a small category mix before random fill.
- [ ] Track applied reward category counts and expose `CurrentGrowthSummaryLabel`.

### Task 4: UI Rendering

**Files:**
- Modify: `Assets/_Game/Scripts/UI/RoomRewardChoiceUI.cs`
- Modify: `Assets/_Game/Scripts/UI/StatusFeedbackUI.cs`

- [ ] Show reward category in the button text.
- [ ] Show preview line between title and description.
- [ ] Resolve `RunManager` in the status panel and render `CurrentGrowthSummaryLabel`.

### Task 5: Verification

- [ ] Refresh Unity and confirm Console has 0 errors.
- [ ] Run `RewardReadabilityTests`.
- [ ] Run all EditMode tests.
- [ ] Play smoke in `SampleScene`: prepared rewards include category/preview data, applying Player and Counterplay rewards updates growth summary.
- [ ] Restore active scene to `Assets/_Game/Scenes/HomeScene.unity`.
- [ ] Run `git -c safe.directory=E:/Unity/Project/AICompanionRoguelike_Demo diff --check`.

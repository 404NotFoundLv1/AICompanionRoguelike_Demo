# v0.40 Expedition Prep Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a home expedition confirmation panel before battle transition.

**Architecture:** Extend the existing `HomeExitPortal` rather than adding a parallel transition component. The portal owns input state, panel drawing, formatting helpers, and still delegates confirmed transition to `EnterBattle()`.

**Tech Stack:** Unity 6000, C#, NUnit EditMode tests, immediate-mode `OnGUI`.

---

### Task 1: Red Tests

**Files:**
- Create: `Assets/Editor/Tests/HomeExpeditionPreparationTests.cs`

- [ ] Add tests for preparation text lines with permanent upgrade levels.
- [ ] Add tests for AI relationship and build tendency text.
- [ ] Add tests for opening and canceling the confirmation panel without transitioning.
- [ ] Run the target EditMode tests and confirm they fail before production changes.

### Task 2: Portal Confirmation State

**Files:**
- Modify: `Assets/_Game/Scripts/Home/HomeExitPortal.cs`

- [ ] Add confirmation state and public read-only state properties.
- [ ] Add public `OpenPreparationPanel`, `CancelPreparation`, and `ConfirmPreparation` methods for testability and UI flow.
- [ ] Update input so E opens the panel first and confirms only when already open.

### Task 3: Preparation Text And UI

**Files:**
- Modify: `Assets/_Game/Scripts/Home/HomeExitPortal.cs`

- [ ] Add `BuildPreparationLines`, `BuildPermanentUpgradeLine`, and `BuildCompanionReadinessLine`.
- [ ] Draw a clamped confirmation panel with confirm/cancel hints.
- [ ] Keep the old one-line prompt when the player is in range but the panel is closed.

### Task 4: Scene Configuration

**Files:**
- Modify: `Assets/_Game/Scenes/HomeScene.unity`

- [ ] Ensure `HomeExitPortal` has confirmation enabled.
- [ ] Set prompt text to a readable prototype string.
- [ ] Save HomeScene.

### Task 5: Verification

- [ ] Run v0.40 target tests.
- [ ] Run all EditMode tests.
- [ ] Play HomeScene and check Console.
- [ ] Play SampleScene and check Console.
- [ ] Restore HomeScene as active scene.
- [ ] Run `git diff --check`.


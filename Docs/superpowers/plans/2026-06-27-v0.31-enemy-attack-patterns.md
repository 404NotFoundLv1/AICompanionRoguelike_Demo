# v0.31 Enemy Attack Patterns Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Give Melee, Ranged, and Guard enemies distinct readable attack mechanics in the playable combat rooms.

**Architecture:** Extend `EnemyAttack2D` with direct/projectile delivery, add a focused projectile component, and add one archetype pattern component for Melee lunge and Guard defense state. Configure everything centrally when `RoomManager` spawns a typed enemy.

**Tech Stack:** Unity 6, C#, Rigidbody2D, SpriteRenderer, NUnit EditMode tests, Unity MCP

---

### Task 1: Lock the behavior contract

**Files:**
- Create: `Assets/Editor/Tests/EnemyArchetypeAttackPatternTests.cs`

- [ ] Add reflection-safe tests proving Melee moves during warning, Ranged launches a visible projectile before damage, moving off the line avoids damage, Guard blocks front damage and becomes vulnerable, rooms wire the correct components, and feedback explains the mechanics.
- [ ] Run EditMode tests and confirm the new cases fail because v0.31 runtime types and configuration are absent.

### Task 2: Implement ranged projectile delivery

**Files:**
- Create: `Assets/_Game/Scripts/Enemy/EnemyAttackDeliveryMode.cs`
- Create: `Assets/_Game/Scripts/Enemy/EnemyProjectile2D.cs`
- Modify: `Assets/_Game/Scripts/Enemy/EnemyAttack2D.cs`

- [ ] Add direct/projectile delivery selection while retaining existing warning and cooldown behavior.
- [ ] Launch a straight visible projectile that applies damage only on segment intersection with its intended target.
- [ ] Report projectile hit or miss through the existing `AttackResolved` event.

### Task 3: Implement Melee and Guard patterns

**Files:**
- Create: `Assets/_Game/Scripts/Enemy/EnemyAttackPattern2D.cs`

- [ ] Subscribe to attack warnings and start a bounded Melee lunge toward the current target.
- [ ] Implement Guard front-facing damage reduction through `IDamageModifier`.
- [ ] Open a timed increased-damage window after Guard windup and update its world marker color.

### Task 4: Wire spawned rooms and feedback

**Files:**
- Modify: `Assets/_Game/Scripts/Enemy/EnemyController2D.cs`
- Modify: `Assets/_Game/Scripts/Enemy/EnemyArchetypeRules.cs`
- Modify: `Assets/_Game/Scripts/Roguelike/RoomManager.cs`
- Modify: `Assets/_Game/Scripts/Roguelike/RunManager.cs`

- [ ] Make controller attack-state range follow the configured attack component.
- [ ] Attach and configure attack delivery and pattern components for non-Boss enemies.
- [ ] Update readable role hints and room entry feedback.

### Task 5: Unity verification

**Files:**
- No scene serialization change expected because spawned enemies are configured at runtime.

- [ ] Compile through Unity MCP and run all EditMode tests.
- [ ] Play a mixed combat room and inspect enemy patterns, projectile state, Guard marker/state, and player damage.
- [ ] Restore `HomeScene`, confirm zero console errors/warnings, and run `git diff --check`.

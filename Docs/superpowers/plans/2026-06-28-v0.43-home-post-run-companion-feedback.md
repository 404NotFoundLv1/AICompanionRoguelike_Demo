# v0.43 Home Post-Run Companion Feedback Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Show a remembered last-run recap and response choices through the existing home AI companion dialogue.

**Architecture:** Add last-run tactic data to `RunSessionSummary`, capture it from `RunSessionState`, and extend `HomeCompanionDialogue` formatting helpers. Keep the interaction on the existing home companion object.

**Tech Stack:** Unity 6000, C#, NUnit EditMode tests, immediate-mode `OnGUI`.

---

### Task 1: Red Tests

**Files:**
- Modify: `Assets/Editor/Tests/HomeCompanionDialogueTests.cs`

- [ ] Add a test that a run ended with Link stores `CompanionSkillTendency.Link` in the summary.
- [ ] Add a test that home dialogue recap includes result, rooms, last room, and AI tactic.
- [ ] Add a test that Discuss Tactics writes Reliable memory and post-run review copy.
- [ ] Run target tests and confirm the new tests fail before production changes.

### Task 2: Summary Data

**Files:**
- Modify: `Assets/_Game/Scripts/Roguelike/RunSessionSummary.cs`
- Modify: `Assets/_Game/Scripts/Roguelike/RunSessionState.cs`

- [ ] Add `CompanionSkillTendency CompanionTactic` to the summary.
- [ ] Preserve older constructor overloads by defaulting the tactic to `Balanced`.
- [ ] Capture `CompanionRunBuildState.CurrentTendency` in `CreateSummary()`.

### Task 3: Home Dialogue Recap

**Files:**
- Modify: `Assets/_Game/Scripts/Home/HomeCompanionDialogue.cs`

- [ ] Add `BuildLastRunReviewLine`.
- [ ] Add result and tactic label helpers.
- [ ] Insert the recap near the top of `BuildDialogueText()`.
- [ ] Adjust Discuss Tactics copy to read as a post-run review.

### Task 4: Scene Configuration

**Files:**
- Modify if needed: `Assets/_Game/Scenes/HomeScene.unity`

- [ ] Ensure the home companion dialogue panel is tall enough for the added recap.
- [ ] Save HomeScene through Unity.

### Task 5: Verification

- [ ] Run the v0.43 target EditMode tests.
- [ ] Run all EditMode tests.
- [ ] Play HomeScene and SampleScene smoke tests and check Console.
- [ ] Run `git diff --check`.


# v0.41 Expedition AI Tactics Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add AI tactic selection to the home expedition preparation panel and preserve the selection into combat.

**Architecture:** Extend `HomeExitPortal` with selection input, deterministic text helpers, and a public selection method for tests. Keep combat behavior wired through `CompanionRunBuildState`; configure the existing combat Build choice UI as a fallback only when no home choice exists.

**Tech Stack:** Unity 6000, C#, NUnit EditMode tests, immediate-mode `OnGUI`, Unity MCP scene configuration.

---

### Task 1: Red Tests

**Files:**
- Modify: `Assets/Editor/Tests/HomeExpeditionPreparationTests.cs`

- [ ] Add tests for default Guardian selection when expedition preparation opens.
- [ ] Add tests for selecting Link from preparation and seeing it in preparation lines.
- [ ] Add tests that the tactical choice lines expose `1/2/3` options.
- [ ] Run the target EditMode tests and confirm they fail before production changes.

### Task 2: Portal Tactical Selection

**Files:**
- Modify: `Assets/_Game/Scripts/Home/HomeExitPortal.cs`

- [ ] Add serialized keys for Guardian, Suppressor, and Link.
- [ ] Reset stale run-build state and select Guardian when opening preparation.
- [ ] Add `SelectPreparationTendency` and `BuildTacticalChoiceLines`.
- [ ] Let `Update` process `1/2/3` while the panel is open.

### Task 3: Preparation UI

**Files:**
- Modify: `Assets/_Game/Scripts/Home/HomeExitPortal.cs`

- [ ] Add tactical choice lines to `BuildPreparationLines`.
- [ ] Draw the current tactic and choice hints in the preparation panel.
- [ ] Keep confirm/cancel hints readable after the extra lines.

### Task 4: Scene Configuration

**Files:**
- Modify: `Assets/Scenes/SampleScene.unity`
- Modify: `Assets/_Game/Scenes/HomeScene.unity`

- [ ] Disable `CompanionBuildChoiceUI.resetBuildOnEnable` in the combat scene so home choices are preserved.
- [ ] Keep `CompanionBuildChoiceUI.showPanel` enabled so direct combat-scene play still allows choosing.
- [ ] Ensure the home portal panel is tall enough for the added choice lines.
- [ ] Save scenes through Unity.

### Task 5: Verification

- [ ] Run the v0.41 target EditMode tests.
- [ ] Run all EditMode tests.
- [ ] Play HomeScene and check Console for errors.
- [ ] Play SampleScene and check Console for errors.
- [ ] Run `git diff --check`.


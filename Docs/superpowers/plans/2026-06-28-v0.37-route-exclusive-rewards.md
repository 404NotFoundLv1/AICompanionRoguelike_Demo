# v0.37 Route Exclusive Rewards Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add route-exclusive rewards after a growth route forms, and show the route result in run completion summaries.

**Architecture:** Keep route state in `RunManager`. Add a dynamic `GrowthRouteSpecialization` reward type whose data is resolved from the active route. Store completion-facing route summary fields in `RunSessionState` and `RunSessionSummary`.

**Tech Stack:** Unity 6000, C#, NUnit EditMode tests, Unity MCP validation.

---

### Task 1: Red Tests

**Files:**
- Create: `Assets/Editor/Tests/RouteExclusiveRewardTests.cs`
- Create: `Assets/Editor/Tests/RouteExclusiveRewardTests.cs.meta`

- [ ] Write tests for route-exclusive reward drafting, effect scaling, and run summary recording.
- [ ] Run only `AICompanionRoguelike.Tests.RouteExclusiveRewardTests` and confirm failures are caused by missing v0.37 APIs and reward type.

### Task 2: Dynamic Route Reward

**Files:**
- Modify: `Assets/_Game/Scripts/Roguelike/RunRewardType.cs`
- Modify: `Assets/_Game/Scripts/Roguelike/RunManager.cs`

- [ ] Add `GrowthRouteSpecialization`.
- [ ] Add route specialization counts and serialized tuning values.
- [ ] Include the dynamic reward only when a route is active.
- [ ] Make reward title, preview, category, and growth tag resolve from the active route.
- [ ] Apply reward by incrementing the active route specialization count.
- [ ] Run route-exclusive reward tests and confirm reward drafting/effect tests pass.

### Task 3: Summary Recording

**Files:**
- Modify: `Assets/_Game/Scripts/Roguelike/RunSessionState.cs`
- Modify: `Assets/_Game/Scripts/Roguelike/RunSessionSummary.cs`
- Modify: `Assets/_Game/Scripts/Roguelike/RunManager.cs`
- Modify: `Assets/_Game/Scripts/UI/HomeRunSummaryUI.cs`

- [ ] Add growth-route summary fields and readable summary line.
- [ ] Record route summary from `RunManager.CompleteRun`.
- [ ] Draw the route summary line in battle completion and home summary UI.
- [ ] Run route-exclusive reward tests and confirm all pass.

### Task 4: Verification

**Files:**
- No new production files.

- [ ] Refresh Unity and confirm compile has no Console errors.
- [ ] Run target tests.
- [ ] Run all EditMode tests.
- [ ] Run short HomeScene and SampleScene Play smoke tests.
- [ ] Restore editor to `Assets/_Game/Scenes/HomeScene.unity`.
- [ ] Run `git diff --check`.

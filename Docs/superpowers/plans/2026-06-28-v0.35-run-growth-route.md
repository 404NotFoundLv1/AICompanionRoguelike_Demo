# v0.35 Run Growth Route Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Convert v0.34 reward category counts into an active in-run growth route that affects later reward choices and is visible to the player.

**Architecture:** Keep route state inside `RunManager` beside existing reward category counters. Extend reward preparation with a route-first candidate pass, expose a route HUD label, and reuse `CompanionSpeechBubbleUI` for route change feedback.

**Tech Stack:** Unity 6000, C#, NUnit EditMode tests, Unity MCP verification.

---

### Task 1: RED Tests

**Files:**
- Create: `Assets/Editor/Tests/RunGrowthRouteTests.cs`

- [ ] Test that two rewards in one category activate `CurrentGrowthRouteLabel`.
- [ ] Test that route-biased reward preparation includes the active category.
- [ ] Test that a stronger category can replace the active route.
- [ ] Run only `RunGrowthRouteTests` and confirm failures mention missing route APIs.

### Task 2: Route State in RunManager

**Files:**
- Modify: `Assets/_Game/Scripts/Roguelike/RunManager.cs`

- [ ] Add active route category, route level, and route label properties.
- [ ] Evaluate route after every applied reward.
- [ ] Reset route state on `StartRun`.
- [ ] Show AI route feedback when route changes.

### Task 3: Route-Biased Reward Preparation

**Files:**
- Modify: `Assets/_Game/Scripts/Roguelike/RunManager.cs`

- [ ] Before the v0.34 category mix, try to add one reward from the active route category.
- [ ] Keep the existing category mix and random fill after that.
- [ ] Avoid duplicate reward choices by removing selected candidates.

### Task 4: HUD Feedback

**Files:**
- Modify: `Assets/_Game/Scripts/UI/StatusFeedbackUI.cs`

- [ ] Add `CurrentGrowthRouteLabel` to the run status panel.
- [ ] Keep `CurrentGrowthSummaryLabel` visible underneath it.

### Task 5: Verification

- [ ] Refresh Unity and confirm Console has 0 errors.
- [ ] Run `RunGrowthRouteTests`.
- [ ] Run all EditMode tests.
- [ ] Play smoke in `SampleScene`: apply two Counterplay rewards, prepare rewards, confirm route label and biased choices.
- [ ] Restore active scene to `Assets/_Game/Scenes/HomeScene.unity`.
- [ ] Run `git -c safe.directory=E:/Unity/Project/AICompanionRoguelike_Demo diff --check`.

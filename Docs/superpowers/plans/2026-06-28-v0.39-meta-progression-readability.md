# v0.39 Meta Progression Readability Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add readable home and run-start feedback for the existing meta progression loop.

**Architecture:** Keep `MetaProgressionState` as the single source of truth. Add one focused home HUD component, extend the upgrade station with visible feedback, and let `RunManager` expose a deterministic applied-upgrade line during run start.

**Tech Stack:** Unity 6000, C#, NUnit EditMode tests, immediate-mode `OnGUI` prototype UI.

---

### Task 1: Red Tests

**Files:**
- Create: `Assets/Editor/Tests/MetaProgressionReadabilityTests.cs`

- [ ] Write tests that expect a `HomeMetaProgressionHUD.BuildStatusLines()` helper.
- [ ] Write tests that expect `HomeMetaUpgradeStation.BuildPurchaseFeedback()` and success/failure state after `TryPurchase`.
- [ ] Write tests that expect `RunManager.BuildMetaProgressionAppliedLine()` to include permanent upgrade levels.
- [ ] Run only `AICompanionRoguelike.Tests.MetaProgressionReadabilityTests` and confirm failure before production edits.

### Task 2: Home HUD And Upgrade Feedback

**Files:**
- Create: `Assets/_Game/Scripts/Home/HomeMetaProgressionHUD.cs`
- Modify: `Assets/_Game/Scripts/Home/HomeMetaUpgradeStation.cs`

- [ ] Add `HomeMetaProgressionHUD` with clamped `OnGUI` panel and deterministic status-line helper.
- [ ] Extend `HomeMetaUpgradeStation.TryPurchase` to store last visible feedback.
- [ ] Add a static purchase feedback formatter so tests and UI share wording.
- [ ] Run target tests and confirm the HUD/station tests pass.

### Task 3: Run-Start Feedback

**Files:**
- Modify: `Assets/_Game/Scripts/Roguelike/RunManager.cs`

- [ ] Add a static applied-upgrades line helper.
- [ ] Set a timed feedback line when permanent upgrades are applied.
- [ ] Draw that line in the existing room feedback panel.
- [ ] Run target tests and confirm all v0.39 tests pass.

### Task 4: Unity Scene Configuration

**Files:**
- Modify: `Assets/_Game/Scenes/HomeScene.unity`

- [ ] Add or update a `HomeMetaProgressionHUD` GameObject in `HomeScene`.
- [ ] Keep `HomeMetaUpgradeStation` prominent and near the player.
- [ ] Save `HomeScene`.

### Task 5: Verification

- [ ] Run all EditMode tests.
- [ ] Enter Play in `HomeScene` and check Console errors/warnings.
- [ ] Enter Play in `Assets/Scenes/SampleScene.unity` and check Console errors/warnings.
- [ ] Restore `HomeScene` as the active scene.
- [ ] Run `git diff --check`.


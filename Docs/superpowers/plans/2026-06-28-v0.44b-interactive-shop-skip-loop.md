# v0.44b Interactive Shop Skip Loop Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace automatic ShopRoom purchase popups with an interactable shop object and a clean skip/close path.

**Architecture:** `RunManager` controls shop state and reward draft lifecycle, `SupplyShopInteractable` opens the shop from the room, and `RoomRewardChoiceUI` exposes a shop-only close action.

**Tech Stack:** Unity 6000.4, C#, NUnit EditMode tests, existing IMGUI runtime panels.

---

### Task 1: Shop Flow Tests

**Files:**
- Modify: `Assets/Editor/Tests/SafeShopSupplyLoopTests.cs`

- [ ] Add tests proving ShopRoom does not auto-open reward choices.
- [ ] Add tests proving `OpenShopRewardDraft` opens purchases only from ShopRoom.
- [ ] Add tests proving `CloseShopRewardDraft` returns to route choice after insufficient supplies.
- [ ] Add tests proving a successful shop purchase prevents a second purchase in the same ShopRoom.

### Task 2: RunManager Shop Lifecycle

**Files:**
- Modify: `Assets/_Game/Scripts/Roguelike/RunManager.cs`

- [ ] Exclude ShopRoom from automatic reward offering.
- [ ] Add public shop draft methods and readable state properties.
- [ ] Preserve route choices while skipping or after buying.
- [ ] Keep insufficient supply feedback without trapping the player.

### Task 3: World Shop Interactable And UI Close

**Files:**
- Create: `Assets/_Game/Scripts/Roguelike/SupplyShopInteractable.cs`
- Modify: `Assets/_Game/Scripts/UI/RoomRewardChoiceUI.cs`

- [ ] Add a world interactable that shows only in ShopRoom and opens the shop draft.
- [ ] Add `Esc` and button close behavior for shop reward UI.
- [ ] Keep normal combat reward UI non-skippable.

### Task 4: Unity Scene Configuration

**Files:**
- Modify: `Assets/Scenes/SampleScene.unity`

- [ ] Add a visible `SupplyShop` object with trigger collider and `SupplyShopInteractable`.
- [ ] Place it away from the next-route portal so the two interactions are readable.
- [ ] Run EditMode tests and Play smoke tests for SampleScene/HomeScene.

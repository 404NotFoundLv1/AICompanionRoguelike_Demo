# v0.30 AI Companion Target Priority Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Give the AI companion enemy-type recognition, stable tactical target priority, and readable target-change feedback.

**Architecture:** Add a pure target-scoring rule and decision result, then make `CompanionSensor` select the highest-scoring valid enemy. Reuse `CompanionCombatDialogueController` and the existing speech bubble for feedback.

**Tech Stack:** Unity 6, C#, Physics2D, NUnit EditMode tests, Unity MCP

---

### Task 1: Lock target priority behavior with tests

**Files:**
- Create: `Assets/Editor/Tests/CompanionEnemyTargetPriorityTests.cs`

- [ ] Write reflection-based tests for ranged priority, Guardian interception, Build-specific weighting, target decision text, and live sensor selection.
- [ ] Run only `CompanionEnemyTargetPriorityTests` and confirm failure because the v0.30 rule types do not exist.

### Task 2: Add pure target-priority rules

**Files:**
- Create: `Assets/_Game/Scripts/Companion/CompanionTargetDecisionReason.cs`
- Create: `Assets/_Game/Scripts/Companion/CompanionTargetPriorityRules.cs`

- [ ] Implement deterministic scoring with distance cost, archetype weight, player-threat weight, Build modifiers, and current-target hysteresis.
- [ ] Implement concise bilingual decision lines for Ranged, player interception, Guard coordination, and default pursuit.
- [ ] Re-run the focused tests and confirm the pure rule cases pass.

### Task 3: Integrate the rules with sensing and dialogue

**Files:**
- Modify: `Assets/_Game/Scripts/Companion/CompanionSensor.cs`
- Modify: `Assets/_Game/Scripts/Companion/CompanionCombatDialogueController.cs`

- [ ] Replace nearest-only selection with highest-score selection while preserving layer/radius/death validation.
- [ ] Expose the current decision reason and score for UI, debugging, and tests.
- [ ] Subscribe dialogue to `TargetChanged` and show the decision line through the existing priority gate.
- [ ] Re-run the focused tests and related companion/enemy suites.

### Task 4: Unity playable verification

**Files:**
- No scene serialization change expected; existing companion components receive the behavior automatically.

- [ ] Refresh and compile scripts through Unity MCP; confirm zero compile errors.
- [ ] Run all EditMode tests.
- [ ] Enter a combat room containing Melee and Ranged enemies, verify the chosen target/reason and visible tactical speech bubble, then restore HomeScene outside Play Mode.
- [ ] Check the Unity console and `git diff --check`.

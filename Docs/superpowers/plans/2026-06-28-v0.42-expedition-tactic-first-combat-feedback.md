# v0.42 Expedition Tactic First Combat Feedback Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Surface the home-selected AI tactic in first combat speech, room feedback, and status HUD.

**Architecture:** Reuse `CompanionRunBuildState` as the state source. Add copy helpers to `CompanionSkillTendencyRules`, then consume those helpers from `CompanionCombatDialogueController`, `RunManager`, and `StatusFeedbackUI`.

**Tech Stack:** Unity 6000, C#, NUnit EditMode tests, immediate-mode `OnGUI`.

---

### Task 1: Red Tests

**Files:**
- Modify: `Assets/Editor/Tests/CompanionBuildTendencyTests.cs`
- Modify: `Assets/Editor/Tests/CombatFeelPolishTests.cs`

- [ ] Add a test that first combat room speech names the selected Link tactic.
- [ ] Add a test that StatusFeedbackUI exposes a Suppressor tactic plan line.
- [ ] Add a test that RunManager room feedback includes the selected tactic and reward influence.
- [ ] Run targeted EditMode tests and confirm the new tests fail before production changes.

### Task 2: Shared Tactic Copy

**Files:**
- Modify: `Assets/_Game/Scripts/Companion/CompanionSkillTendencyRules.cs`

- [ ] Add `GetRunLaunchLine`.
- [ ] Add `GetRunFeedbackLine`.
- [ ] Add `GetRunClearRewardLine`.
- [ ] Add `GetStatusPlanLine`.

### Task 3: First Room Dialogue

**Files:**
- Modify: `Assets/_Game/Scripts/Companion/CompanionCombatDialogueController.cs`

- [ ] Track whether the first tactic line has already been spoken.
- [ ] On the first non-branch room start, show `GetRunLaunchLine` with higher priority than the generic room-start line.
- [ ] Fall back to the existing room-start line for later rooms.

### Task 4: Room And HUD Feedback

**Files:**
- Modify: `Assets/_Game/Scripts/Roguelike/RunManager.cs`
- Modify: `Assets/_Game/Scripts/UI/StatusFeedbackUI.cs`

- [ ] Append `GetRunFeedbackLine` to combat room start feedback.
- [ ] Append `GetRunClearRewardLine` to combat room clear feedback when a reward or route choice follows.
- [ ] Draw `GetStatusPlanLine` in the status panel.

### Task 5: Unity Configuration And Verification

**Files:**
- Modify if needed: `Assets/Scenes/SampleScene.unity`

- [ ] Ensure SampleScene has `StatusFeedbackUI`, `CompanionCombatDialogueController`, and `CompanionSpeechBubbleUI` active.
- [ ] Run v0.42 targeted EditMode tests.
- [ ] Run all EditMode tests.
- [ ] Play HomeScene and SampleScene smoke tests and check Console.
- [ ] Run `git diff --check`.

